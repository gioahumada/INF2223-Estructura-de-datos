#include <stdio.h>

struct Loteria {
    struct Carton **cartones;
    int pLibre;
};

struct Carton {
    int numeroSorteo;
    int dia;
    int mes;
};

struct Carton *getCartonNumeroGanador(struct Loteria *loto, int numGanador) {
    /* Funcion a crear */
    /* Funcion a crear */
    /* Funcion a crear */
}

int main() {
    struct Loteria *loto;
}